{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "description": "Server implementation of the TMS",
  "main": "build/server.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "prestart": "yarn build",
    "start": "nodemon .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.0"
  }
}
