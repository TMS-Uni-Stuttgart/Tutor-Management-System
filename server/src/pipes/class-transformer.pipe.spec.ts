import { ClassTransformerPipe } from './class-transformer.pipe';

describe('ClassTransformerPipePipe', () => {
    it('should be defined', () => {
        expect(new ClassTransformerPipe()).toBeDefined();
    });
});
