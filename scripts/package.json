{
  "name": "scripts",
  "version": "1.0.0",
  "description": "Some scripts for the TMS",
  "main": "index.js",
  "private": true,
  "scripts": {
    "ts:lint": "eslint \"**/*.ts?(x)\"",
    "ts:fix": "eslint --fix \"**/*.ts?(x)\"",
    "ts:check": "tsc --project . --noEmit",
    "ts:check:watch": "tsc --project . --noEmit -w",
    "build": "tsc --project ."
  },
  "author": "Sascha",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.19.0",
    "faker": "^4.1.0"
  },
  "devDependencies": {
    "@types/faker": "^4.1.5"
  }
}
