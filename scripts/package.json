{
  "name": "scripts",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "eslint:check": "eslint \"./**/*.ts?(x)\"",
    "format": "eslint --fix \"./**/*.ts?(x)\"",
    "prettier:check": "prettier --check \"./**/*.ts?(x)\"",
    "generateData": "tsx -r tsconfig-paths/register ./generate-data/generateData.ts",
    "ts:check": "tsc --project . --noEmit",
    "ts:check:watch": "tsc --project . --noEmit -w"
  },
  "devDependencies": {
    "@types/color-convert": "^2.0.3",
    "axios": "^1.7.3",
    "tsx": "^4.19.1",
    "tsconfig-paths": "^4.2.0"
  },
  "dependencies": {
    "color-convert": "^2.0.1"
  }
}
